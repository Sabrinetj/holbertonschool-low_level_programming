#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @argc: number of argument passed
 * @argv: array pointing to arguements
 * Return: 0 (Success)
 */
int main(int argc, char *argv[])
{
	argc = 0;
		printf("%s\n", argv[argc]);
	return (0);
}
