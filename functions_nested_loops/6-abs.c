#include "main.h"
/**
 * _abs - function that return the absulute value of int
 * @n: the numebr to treat
 *
 * Return: the absulute number
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
