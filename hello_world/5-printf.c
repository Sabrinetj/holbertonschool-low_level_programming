#include <stdio.h>
/**
 * main - Print a simple message folowwed by a new line with printf function
 *
 * Return: (0)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
